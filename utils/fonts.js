export const fonts = {
  // Note: These configs apply the selected Cairo font sizes and weights.
  // Ensure the Cairo font is correctly loaded using expo-font in the app entry.
  title: {
    fontFamily: 'Cairo-Bold',
    fontSize: 32,
    fontWeight: '700',
  },
  header: {
    fontFamily: 'Cairo-SemiBold',
    fontSize: 24,
    fontWeight: '600',
  },
  regular: {
    fontFamily: 'Cairo-Regular',
    fontSize: 16,
    fontWeight: '400',
  },
  bold: {
    fontFamily: 'Cairo-Bold',
    fontSize: 16,
    fontWeight: '700',
  }
};
